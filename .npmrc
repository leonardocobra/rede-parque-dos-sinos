# @vercel/speed-insights declara @sveltejs/kit como peer OPCIONAL; o resolver do
# npm tenta satisfazê-lo e colide com o vite do Vitest. Como este é um projeto
# Next (não Svelte), relaxamos a checagem de peers para um install consistente
# aqui e na Vercel.
legacy-peer-deps=true
